services:
  - type: web
    name: workspace-gpt
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd workspace-gpt/backend && uvicorn main:app --host 0.0.0.0 --port $PORT